@model BankStatementAnalyzer.BusinessLogic.ViewModels.OrderVM
@{
    ViewBag.Title = "Details";
    ViewBag.MainHeading = "Order Details";
    ViewBag.BoxHeader = "Details";
}

@{ Html.RenderPartial("OrderDetailPartial", Model);}

@using (Html.BeginForm("AssignToVendor", "Orders", new { id = Model.OrderId, redirectAction = Model.RedirectAction }, FormMethod.Post))
{
    <div class="row no-print">
        <div class="col-12">
            <div class="form-horizontal">
                <div class="form-group">
                    @Html.Label("Vendors", htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-6">
                        @Html.DropDownListFor(model => model.VendorId, (IEnumerable<SelectListItem>)ViewBag.Vendor, "Select Vendor", new { @class = "form-control", @autocomplete = "Off" })
                        @Html.ValidationMessageFor(model => model.VendorId, "", new { @class = "text-danger" })
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row no-print">
        <div class="col-12">
            <button type="submit" class="btn btn-info ml-15">
                <i class="fa fa-check"></i>Assign To Vendor
            </button>
        </div>
    </div>
}